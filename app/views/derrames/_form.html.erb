<%= form_for(@derrame) do |f| %>
  <% if @derrame.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@derrame.errors.count, "error") %> prohibited this derrame from being saved:</h2>

      <ul>
      <% @derrame.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
